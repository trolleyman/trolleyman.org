syntax = "proto3";

package facebook;

message LoginDetails {
	string email = 1;
	string password = 2;
}

message LoginToken {
	string token = 1;
}

message Echo {
	string payload = 1;
}

service Facebook {
	rpc login (LoginDetails) returns (LoginToken);
	rpc echo (Echo) returns (Echo);
}
