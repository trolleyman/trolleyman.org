syntax = "proto3";

message LoginDetails {
	string email = 1;
	string password = 2;
}

message LoginToken {
	string token = 1;
}

service FacebookSrv {
	rpc login (LoginDetails) returns (LoginToken);
}
